name: CI Maximo Project Base nextjs

on:
  push:
    branches:
      - main
      - develop
      - project_base
  pull_request:
    branches:
      - main
      - develop
      - project_base

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18.18.0' # Actualiza a una versión compatible

    - name: Install dependencies
      run: npm install

    - name: Run lint
      run: npm run lint

    - name: Run build
      run: npm run build

    - name: Export static site
      run: npm run export

    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: build-artifacts
        path: out/

    - name: Deploy to GitHub Pages
      if: github.ref == 'refs/heads/project_base'
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./out